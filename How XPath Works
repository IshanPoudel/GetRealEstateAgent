for  //*[@id="mat-input-32"]

 // means from all over , means any tag name with the attribute id with the value mat-input-23

in the new version you need the have the tuple and send it to the find_element attribute


CSS_TUPLE = (By.CSS_SELECTOR , "#gh-ac")
abc = driver.find_element(*CSS_TUPLE).send_keys("Video");
driver.implicitly_wait(10)